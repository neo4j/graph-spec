{
  "name": "ontology-spec",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Neo4j Ontology Specification v1 - JSON Schema and examples (draft)",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "generate": "node scripts/generate-yaml.mjs",
    "prevalidate": "node scripts/generate-yaml.mjs",
    "validate": "node scripts/validate.mjs",
    "convert": "node scripts/ttl2ontology.mjs"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "yaml": "^2.8.1"
  },
  "dependencies": {
    "n3": "^2.7.12"
  }
}
