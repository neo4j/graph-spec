version: "4.0.0"
nodes:
  document:
    label: "document"
    properties:
      documentid: { type: "INTEGER", key: true }
      embedding: { type: "VECTOR<FLOAT>", dimension: 123, mustExist: true }
      legacyembedding: { type: "VECTOR<FLOAT>", mustExist: true }
    indexes:
      document_embedding:
        type: "VECTOR"
        labels: ["document"]
        properties: ["embedding"]
tables:
  document:
    source: "cloud"
    fields:
      documentid:
        type: "INT"
        suggested: "INTEGER"
        supported: ["INTEGER"]
      embedding:
        type: "VECTOR"
        suggested: "VECTOR<FLOAT>"
        supported: ["VECTOR<FLOAT>", "VECTOR<FLOAT32>"]
        dimension: 123
      legacyembedding:
        type: "VECTOR"
        suggested: "VECTOR<FLOAT>"
        supported: ["VECTOR<FLOAT>"]
    primaryKeys: ["documentid"]
mappings:
  - node: "document"
    table: "document"
    properties:
      documentid: { field: "documentid" }
      embedding: { field: "embedding" }
      legacyembedding: { field: "legacyembedding" }
    key: ["documentid"]
